version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: web.Dockerfile
      args:
        - VITE_API_KEY
    environment:
      - VITE_API_KEY
    ports:
      - "80:80"
    env_file:
      - .env